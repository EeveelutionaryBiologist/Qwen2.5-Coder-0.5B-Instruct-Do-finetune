# Training environment.
# Target: single RTX 4090, Python >= 3.10. Not this machine.
#
# Install a CUDA build of torch on the training box, e.g.:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# NOT included: vLLM (§4, teacher generation). It pins torch aggressively and
# belongs in its own environment.
# Use uv!

torch~=2.14
transformers~=5.17          # v5, per §2
trl~=1.13.0                 # pinned to the minor: they are pruning old APIs
datasets~=5.0
accelerate~=1.15

# 8-bit optimizer states (optim="adamw_bnb_8bit"). Required for the 1.5B run.
bitsandbytes~=0.50.2

# Optional run logging (the legacy script uses it).
wandb~=0.30
